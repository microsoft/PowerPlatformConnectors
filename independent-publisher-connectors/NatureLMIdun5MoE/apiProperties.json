{
  "properties": {
    "connectionParameters": {
      "tenantId": {
        "type": "string",
        "uiDefinition": {
          "displayName": "Microsoft Entra tenant ID",
          "description": "The directory tenant ID of the Entra tenant that owns the Foundry resource.",
          "tooltip": "Format: a GUID, e.g. 00000000-0000-0000-0000-000000000000",
          "constraints": {
            "required": "true"
          }
        }
      },
      "tokenScope": {
        "type": "string",
        "uiDefinition": {
          "displayName": "OAuth scope",
          "description": "Entra resource scope for the bearer token. Must end in /.default.",
          "tooltip": "Default is https://ai.azure.com/.default"
        },
        "default": "https://ai.azure.com/.default"
      }
    },
    "iconBrandColor": "#da3b01",
    "capabilities": [],
    "publisher": "Alexander Kleine",
    "stackOwner": "Alexander Kleine"
  }
}
