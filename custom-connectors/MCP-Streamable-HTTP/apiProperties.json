{
  "properties": {
    "connectionParameters": {
      "mcpServerURL": {
        "type": "string",
        "uiDefinition": {
          "constraints": {
            "required": "true"
          },
          "description": "Specify the root URL of your MCP Server",
          "displayName": "MCP Server root URL",
          "tooltip": "Specify the root URL of your MCP Server (so without the /mcp part or the https:// part)"
        }
      }
    },
    "iconBrandColor": "#007ee5",
    "capabilities": [ ],
    "policyTemplateInstances": [
      {
        "templateId": "dynamichosturl",
        "title": "Route to MCP Server",
        "parameters": {
          "x-ms-apimTemplateParameter.urlTemplate": "https://@connectionParameters('mcpServerURL')",
          "x-ms-apimTemplate-operationName": [ ]
        }
      }
    ]
  }
}
